import { View, Text } from 'react-native'
import React from 'react'
import SafeScreen from '@/components/SafeScreen'

const AddressesScreen = () => {
  return (
    <SafeScreen>
      <Text className="text-white">AddressesScreen</Text>
    </SafeScreen>
  )
}

export default AddressesScreen